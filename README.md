# Loom
*Loom* is a programmable code-generation tool for generating fully customizable code archetypes using Lua.


### autodetect.lua
Does not have access to the _loom api_, you can only use constants `LOOM_VERSION` and `LOOM_PROJECT_ROOT`
